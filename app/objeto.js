// Crear un objeto con un método sumar
export const calculadora = {
    // Método para sumar dos números
    sumar(a, b) {
        return a + b;
    },

    // Método para restar dos números
    restar(a, b) {
        return a - b;
    }
};