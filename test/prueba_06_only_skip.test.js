const assert = require('chai').assert;

describe('Suite de prueba con pruebas seleccionadas y omitidas', () => {
    /*

    it('Prueba 1', () => {
        assert.equal(1 + 1, 2);
    });
    Cuidado con estos only porque marcan toda
    la ejecición
    it.only('Prueba 2 (seleccionada)', () => {
        assert.equal(2 * 3, 6);
    });

    it.skip('Prueba 3 (omitida)', () => {
        assert.equal(4 / 2, 2);
    });

     */
});
